# Cobros y onboarding · Cohorte Octubre (30X)

Sitio estático generado. Los datos de clientes van cifrados (AES-256-GCM, clave derivada con PBKDF2) en `datos.enc`
y solo se abren en el navegador con la clave. Este repo no contiene código fuente ni datos en claro.
Se regenera y publica solo cada día a las 7:00 (hora de Colombia).
